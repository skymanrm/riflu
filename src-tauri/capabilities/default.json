{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs, window dragging and closing, tray events, and opening the Yandex sign-in page in the system browser",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging",
    "core:window:allow-close",
    "opener:default",
    "opener:allow-open-url"
  ]
}
